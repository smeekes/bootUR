# Printing Summary Output for Objects of class bootUR

This function prints summary output for objects of class bootUR (for
unit root testing)

## Usage

``` r
# S3 method for class 'bootUR'
print(x, digits = max(3, getOption("digits") - 3), ...)
```
